# Base
a = input()
command = "/icc.pp/libr/hexo/main.ii/def.h/spamserverdata/i.h/qsert.h"

# Spam function from hexopy, hexocode, icc.pp
def spam():
  while a:
    print("Data >>> _module_<int<<main>>>{spamserv}.Running: _Spam"

# Checker 
if a == command:
  spam()
else:
	print("Wrong task: Data >>> _MODULE_.connectionToServers = false<int<<nil>>>")
